Steps to run locally (scaffold, no external services required):
1. cd frontend && npm install && npm run dev
2. cd backend && npm install && npm run dev
Frontend will use mock routes at http://localhost:5000 by default
