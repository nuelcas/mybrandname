# Backend

Tech: Node.js + Express + TypeScript (scaffolded, mock routes)

## Scripts
- `npm run dev` - start dev server with ts-node-dev
- `npm run build` - compile
- `npm start` - run compiled build

Copy `.env.example` to `.env` and update variables before running.
